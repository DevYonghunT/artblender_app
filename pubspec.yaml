name: artblender
description: "artblender_app"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  screenshot: ^3.0.0
  image_picker: ^1.0.7
  image_gallery_saver: ^2.0.3
  camera: ^0.11.0 # 카메라 플러그인 추가

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
